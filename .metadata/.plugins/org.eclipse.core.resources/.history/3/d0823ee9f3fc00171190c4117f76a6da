package jadenCase;

import java.util.Arrays;

public class JadenCase {

	public String jadenCase(String str) {
		System.out.println("str: " + str);
		
		if (str == null)
			return str;
		
		String[] result = str.split("\\s");
		System.out.println("length: " + result.length);
		System.out.println(Arrays.toString(result).substring(1));
		return null;
		//return Character.toUpperCase(result[0].charAt(0)) + jadenCase(result.);
		
//		System.out.println("length: " + str.split("\\s").length);
//		String prefix = str.substring(0, str.split("\\s").length);
//		String surfix = str.substring(str.split("\\s").length + 1, str.length());
//		System.out.println("prefix: " + prefix);
//		System.out.println("surfix: " + surfix);
//
//		return Character.toUpperCase(prefix.charAt(0)) + str.substring(1, str.split("\\s").length) + jadenCase(surfix);
	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		JadenCase jc = new JadenCase();

		System.out.println("yes i can do");
		//System.out.println(jc.jadenCase("a b c d e f"));
		System.out.println(jc.jadenCase("3people unFollowed me for the last week"));
	}

}
