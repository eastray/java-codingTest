package problem9;

public class Pythagoras {

	private int pythagoras(int n) {
		int a = 3;
		int b = 4;
		int c = n - a - b;
		int i = 0;
		int j = 0;
		int k = 0;

		// for (int i = 2; i < n/2; i++) {
		// while(true) {
		//
		// if (Math.pow((i * a), 2) + Math.pow((i * b), 2) == Math.pow((i * c),
		// 2)) {
		// System.out.println("Math.pow((i * a), 2): " + Math.pow((i * a), 2) +
		// ", Math.pow((i * b), 2): "
		// + Math.pow((i * b), 2) + ", Math.pow((i * c), 2): " + Math.pow((i *
		// c), 2));
		//
		// System.out.println("(i*a): " + (i * a) + ", (i*b): " + (i * b) + ",
		// (i*c): " + (i * c) + " : "
		// + ((i * a) + (i * b) + (i * c)));
		// }
		//
		// if(1008 == ((i * a) + (i * b) + (i * c)))
		// return ((i * a) * (i * b) * (i * c));
		//
		// i++;
		// }

		// if (1000 == ((i + a) + (l + b) + (k + c))) {
		// System.out.println("(i + a): " + (i + a) + ", (l + b): " + (l + b) +
		// ", (k + c): " + (k + c));
		// System.out.println(((i + a) * (l + b) * (k + c)));
		// return ((i + a) + (l + b) + (k + c));
		// }
		//
		// if (Math.pow((i + a), 2) + Math.pow((l + b), 2) == Math.pow((k + c),
		// 2))
		// System.out.println("Math.pow((i * a), 2): " + Math.pow((i * a), 2) +
		// ", Math.pow((l * b), 2): "
		// + Math.pow((l * b), 2) + ", Math.pow((k * c), 2): " + Math.pow((k *
		// c), 2));
		// }

		while (true) {

			if (Math.pow((i + a), 2) + Math.pow((j + b), 2) == Math.pow((k + c), 2))
				System.out.println("Math.pow((i + a), 2): " + Math.pow((i + a), 2) + ", Math.pow((j + b), 2): "
						+ Math.pow((j + b), 2) + ", Math.pow((k + c), 2): " + Math.pow((k + c), 2));
			else if (Math.pow((i + a), 2) + Math.pow((j + b), 2) < Math.pow((k + c), 2)){
				c--;
				b++;
			} else if( Math.pow((i + a), 2) + Math.pow((j + b), 2) > Math.pow((j + c), 2)){
				a++;
				b = a +1;
				c = n - a - b;
			}
			
			if(b >= c){
				a++;
				b = a +1;
				c = n - a - b;
			}
				
			if(b == 499)
				break;
			
			System.out.println("a: " + a + ", b: " + b +", c: " + c );
				
			
		}
		return 0;

	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Pythagoras pytha = new Pythagoras();

		System.out.println("result: " + pytha.pythagoras(1000));
	}

}
