package problem9;

public class Pythagoras {

	private int pythagoras(int n) {
		int a = 3;
		int b = 4;
		int c = 5;
		int i = 2;

		//for (int i = 2; i < n/2; i++) {
		while(true) {
			if(1008 == ((i * a) + (i * b) + (i * c)))
				return ((i * a) * (i * b) * (i * c));
			
			if (Math.pow((i * a), 2) + Math.pow((i * b), 2) == Math.pow((i * c), 2)) {
				System.out.println("Math.pow((i * a), 2): " + Math.pow((i * a), 2) + ", Math.pow((i * b), 2): "
						+ Math.pow((i * b), 2) + ", Math.pow((i * c), 2): " + Math.pow((i * c), 2));

				System.out.println("(i*a): " + (i * a) + ", (i*b): " + (i * b) + ", (i*c): " + (i * c) + " : "
						+ ((i * a) + (i * b) + (i * c)));
			}
			
			i++;
		}

	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Pythagoras pytha = new Pythagoras();

		System.out.println("result: " + pytha.pythagoras(1000));
	}

}
